Folder in which the unit tests and integration tests are contained.

To run these tests, we use the `PyTest` module. 
Install it using `pip`.

Then, run the command

```
py.test
```

and check the output.
